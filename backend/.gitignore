node_modules
dist
.env
.env.local
.env.*.local

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Prisma
# (You might want to ignore the generated client if you are generating it into a custom folder, but typically it goes into node_modules so it's fine)
*.db
*.sqlite

# OS Files
.DS_Store
Thumbs.db
